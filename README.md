# Color Palette Studio

Modern, super fast color palette generator built with **React + Vite** and designed to work perfectly on **GitHub Pages**.

Generate, lock and export color palettes for your UI designs, Tailwind configs, brand guidelines or Dribbble shots.

---

## ✨ Features

- 🎲 **Random palettes** – 5 colors at a time
- 🔒 **Lock any color** so it doesn't change on next generate
- 📋 **Copy HEX** with one click
- 💾 **Save palettes** to `localStorage` (per browser)
- 🧾 **Export JSON** with the current palette
- 🖼️ **Export PNG** – stripe image with all colors + HEX values
- 🌗 **Light / dark mode** toggle (stored in `localStorage`)
- ⚡ Built with **React + Vite** – instant reloads and small bundle

---

## 🛠 Tech stack

- React 18
- Vite
- Vanilla CSS (no UI framework, easy to customize)

---

## 🚀 Running locally

```bash
git clone https://github.com/mysterekkk/color-palette-studio.git
cd color-palette-studio
npm install
npm run dev

Then open:
http://localhost:5173

---
## 📦 Build
npm run build

The production build will be in the dist/ folder.